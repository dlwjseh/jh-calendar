# 단계 2: 반복 규칙 데이터 모델

## 학습 목표
- 반복 규칙을 표현하는 `RecurrenceRule` **raw value enum** 을 정의한다.
- `Event` (`@Model`) 에 `recurrence` 필드를 추가하고, SwiftData 가 **enum 을 어떻게 저장**하는지 이해한다.
- 기존에 저장된 이벤트가 깨지지 않도록 **lightweight migration**(기본값) 을 처리한다.

> 이 단계는 화면 변화가 거의 없다 (솔직히 안내). 대신 enum + 영속화는 따로 배울 가치가 커서 독립 단계로 둠.

## 사전 산출물
- `EventModels.swift` 의 `Event` `@Model` (09단계).
- enum + 연관값은 이미 봄 → [10-02](../10-이벤트-추가-다이얼로그/02-AddEventDialog%20UI.md) 의 `EventDialogMode`(`.add(Date)` / `.edit(Event)`).

## Swift 개념

### raw value enum (`enum X: String`)
10단계의 `EventDialogMode` 는 **연관값(associated value)** enum 이었다 — 케이스마다 다른 데이터를 품는다. 이번엔 **raw value** enum — 각 케이스가 고정된 "원시값"(String/Int 등)에 1:1로 매핑된다.

```swift
enum RecurrenceRule: String, Codable, CaseIterable {
    case none, daily, weekly, monthly, yearly
}
```

- `: String` → 각 케이스의 raw value 는 케이스 이름 문자열(`"none"`, `"daily"` …). `RecurrenceRule(rawValue: "daily")` 로 역변환, `.daily.rawValue` 로 문자열.
- **Java 비교**: Java enum 은 그 자체가 완전한 클래스(필드·메서드 가능). Swift 의 raw value enum 은 그보다 가벼워서, Java 의 `name()` / `valueOf(String)` 에 해당하는 매핑을 **언어가 공짜로** 준다. (Swift enum 도 메서드/연산프로퍼티는 가질 수 있다 — 03단계에서 라벨 메서드를 붙일 것.)

### `CaseIterable`
`CaseIterable` 채택 → `RecurrenceRule.allCases` 가 `[.none, .daily, .weekly, .monthly, .yearly]` 배열로 **자동 생성**된다. 03단계 피커에서 옵션 목록으로 그대로 쓴다. (Java 의 `values()` 와 같은 결.)

### `Codable`
JSON 직렬화 호환용. raw value enum 은 어차피 자동 Codable 이지만 명시해 두면 의도가 분명. (18단계 `Codable` 와 동일 개념.)

### SwiftData 가 enum 을 저장하는 법
SwiftData 는 **`RawRepresentable` 이고 raw type 이 저장 가능 타입(String/Int 등)** 인 enum 을 `@Model` 프로퍼티로 **그냥** 받는다. 별도 변환기 필요 없음 — 내부적으로 raw value(String)로 컬럼에 저장.

```swift
@Model
final class Event {
    // ...기존 필드...
    var recurrence: RecurrenceRule = .none   // ← 추가 (기본값 필수)
}
```

> **함정 — 마이그레이션**: 이미 디스크에 저장된 기존 이벤트들은 `recurrence` 컬럼이 없다. 새 필드에 **기본값(`= .none`)** 을 주면 SwiftData 가 lightweight migration 으로 기존 행을 `.none` 으로 채워 호환한다. 기본값을 안 주면 스키마 불일치로 **실행 시 크래시**할 수 있다. (Spring 의 Flyway/Liquibase 없이 JPA `ddl-auto` 가 컬럼 추가해 주는 것과 비슷한 결 — 단 기본값을 우리가 명시.)

## 구현 가이드
- `RecurrenceRule.swift` 새 파일(`Features/Event/`) 또는 `EventModels.swift` 안에 enum 정의.
- `Event` 에 `var recurrence: RecurrenceRule = .none` 추가.
- `Event.init(...)` 에 `recurrence: RecurrenceRule = .none` 파라미터 추가 (기본값 줘서 기존 호출부 안 깨지게).

```swift
init(name: String, isAllDay: Bool, category: Category? = nil,
     startDate: Date, endDate: Date,
     recurrence: RecurrenceRule = .none) {   // ← 추가
    // ...
    self.recurrence = recurrence
}
```

## 직접 구현하기
- [ ] `RecurrenceRule` enum 정의 (`: String, Codable, CaseIterable`)
- [ ] `Event` 에 `recurrence` 필드 + 기본값
- [ ] `init` 에 `recurrence` 파라미터(기본값 `.none`) 추가
- [ ] 빌드 통과 (기존 `Event(...)` 호출부가 안 깨지는지)

## 자가 점검
- 빌드 통과?
- 기존에 만들어 둔 이벤트들이 앱 재실행 후에도 정상 표시되는가? (마이그레이션 확인)
- 임시 확인: 저장 코드에서 잠깐 `recurrence: .weekly` 하드코딩 → 이벤트 저장 → 앱 재시작 → 그 이벤트의 `recurrence` 가 `.weekly` 로 유지되는지 (`print` 로). **확인 후 하드코딩은 되돌릴 것.**
- 퀴즈: `EventDialogMode`(연관값)와 `RecurrenceRule`(raw value)의 차이는? — 케이스가 데이터를 품느냐(연관값) vs 고정 원시값에 매핑되느냐(raw value).

## Claude 리뷰 체크리스트
- [ ] raw value enum + `CaseIterable` 관용적으로 선언
- [ ] `Event.recurrence` 기본값 `.none` (마이그레이션 안전)
- [ ] `init` 파라미터 기본값으로 기존 호출부 비파괴
- [ ] 아직 UI/렌더링은 안 건드림 (그건 03·04)

## 회고
- 막혔던 부분?
- 추가로 궁금했던 점?
> *(직접 채우는 영역)*

## 조금 더 (선택)
- raw value 를 Int 로 두는 선택지도 있다 (정렬·저장 약간 가벼움). 다만 String 이 디버깅·마이그레이션 때 사람이 읽기 좋다.
- 나중에 "종료일/횟수" 를 넣는다면 `RecurrenceRule` 을 다시 **연관값** enum(`case weekly(until: Date?)`)으로 키우거나 별도 필드로 뺄 수 있다. 지금은 단순 raw value 로 충분.
