# 단계 5: 카테고리 추가 저장

## 작업 목표
`AddCategoryDialog` 가 선택된 폴더에 실제 `Category` 를 추가하고, 색상까지 영속화되게 만든다. 사이드바가 즉시 갱신되는 것까지 확인.

> 이 단계는 4단계 (폴더 추가 저장) 의 패턴 반복 + 관계 연결이 추가될 뿐. 새 Swift 개념은 거의 없다.

## 사전 지식
- 4단계 산출물 — `modelContext.insert(...)` 패턴이 익숙해진 상태
- `@Environment(\.modelContext)` → [04-폴더 추가 저장.md](./04-폴더%20추가%20저장.md) 참고
- `@Relationship` 양방향 관계 → [02-Category @Model + Relationship.md](./02-Category%20@Model%20%2B%20Relationship.md) 참고

## 작업 가이드

### 두 가지 등록 방식 — 어느 쪽을 골라도 결과는 같다

**A. 폴더의 컬렉션에 직접 추가 (권장)**
```swift
let category = Category(name: trimmed, color: selectedColor, isChecked: true)
selectedFolder.categories.append(category)
// SwiftData 는 관계로 연결된 새 인스턴스도 자동 인식 → 별도 insert 불필요
```

**B. 명시적 insert + folder 세팅**
```swift
let category = Category(name: trimmed, color: selectedColor, isChecked: true)
category.folder = selectedFolder
modelContext.insert(category)
```

> Spring JPA 의 "양방향 관계는 양쪽 다 세팅" 권장과 같은 결. A 가 더 SwiftUI 스럽지만, B 가 더 명시적. 한쪽으로 일관되게 가자.

### 파일 변경 계획
- `AddCategoryDialog.swift`
  - `@Environment(\.modelContext) private var modelContext` 추가
  - 저장 버튼의 `print(...)` 를 실제 등록 로직으로 교체
  - 폴더 선택 상태는 이미 1단계에서 `@State private var selectedFolder: Folder?` 로 잡혀 있음 (인스턴스 직접 보유). lookup 불필요 — `selectedFolder` 그대로 사용.

### 핵심 골격

```swift
struct AddCategoryDialog: View {
    @Binding var isPresented: Bool
    @Environment(\.modelContext) private var modelContext
    let folders: [Folder]
    @State private var selectedFolder: Folder? = nil   // 1단계에서 이미 이렇게 잡혀 있음
    ...

    private func save() {
        let trimmed = name.trimmingCharacters(in: .whitespaces)
        guard let folder = selectedFolder, !trimmed.isEmpty else { return }
        let category = Category(name: trimmed,
                                color: selectedColor,
                                isChecked: true)
        folder.categories.append(category)
    }
}
```

### 막힐 만한 지점 — 힌트

- **`selectedFolder` 가 이미 인스턴스로 잡혀 있다** — 1단계에서 `selectedFolderID: UUID?` → `selectedFolder: Folder?` 로 전환 완료. `folders.first(where:)` 같은 lookup 도 필요 없고, `Menu` 안 `Button` 액션도 이미 `selectedFolder = folder` 로 인스턴스를 직접 잡고 있음. 이 단계에선 그 인스턴스를 그대로 `save()` 에서 사용하면 됨.
- **카테고리가 추가됐는데 사이드바 색깔이 회색/검정** — Color → hex 변환에서 sRGB 변환을 빼먹었을 가능성. [03-Color 영속화.md](./03-Color%20영속화.md) 의 sRGB 변환 부분 재점검.
- **선택 폴더가 nil 인데 저장 버튼이 활성화** — `isSaveEnabled` 는 이미 `selectedFolder != nil` 로 1단계에서 정리됨. 혹시 다시 손댄 흔적이 있으면 점검.

## 직접 구현하기
- [x] `AddCategoryDialog` 가 `modelContext` 를 환경에서 받음 (※ A 방식만 쓰면 실제로는 불필요 — 06 삭제 단계에서 직접 사용 시점에 다시 도입 예정이라 지금은 미사용 줄로 남아 있음)
- [x] 저장 버튼이 실제로 카테고리를 `selectedFolder` 에 등록 (A 방식 — `selectedFolder?.categories.append(category)`)
- [x] 저장 직후 사이드바가 즉시 갱신되며 새 카테고리가 보임
- [x] 앱 재시작 후에도 카테고리·색상·체크 상태가 유지됨

## 자가 점검
- 카테고리 추가 → 사이드바에 즉시 나타나는가?
- 12색 팔레트에서 고른 색이 실제 사이드바 점색과 일치하는가?
- 체크박스 토글이 재시작 후에도 유지되는가? (`isChecked` 자동 dirty tracking 검증)
- 이해도 퀴즈
  1. `folder.categories.append(c)` 만 했는데 왜 `modelContext.insert(c)` 를 부르지 않아도 저장되는가?
  2. 만약 `Category` 가 `Folder` 와 관계가 없는 독립 모델이었다면 어떤 차이가 있을까?

## Claude 리뷰 체크리스트
- [x] 카테고리 등록 방식이 일관됨 (A 방식 — `selectedFolder?.categories.append`)
- [x] 색상이 hex 변환을 거쳐 저장되고 UI 에서 손실 없이 복원됨 (`Category.init` 에서 `color.toHex()`)
- [x] 사이드바가 즉시 갱신 (@Query 자동 재조회 확인)
- [x] 재시작 후 영속화 검증

## 회고
> *(직접 채우는 영역)*

## 조금 더 (선택)
- 다중 선택으로 한 번에 여러 카테고리 추가하는 패턴 (이번 앱 범위는 아님).
