# JHCalendar — 학습 주도 개발 가이드

이 파일은 Claude Code 가 매 세션 자동으로 읽는 프로젝트 지침이다.

---

## 프로젝트 목적

- **1차 목표**: 이 앱 개발을 통해 **Swift / SwiftUI 학습**
- **2차 목표**: macOS 캘린더 앱 완성
- 학습이 우선이므로, **빠른 구현보다 작은 단위로 쪼개 개념을 익히는 것** 을 우선한다.

## 사용자 배경

- **메인 스택**: Java, Spring Boot (백엔드 개발자)
- **부가 경험**: Vue.js, JavaScript 약간
- **그 외 언어/프레임워크는 잘 모름** — TypeScript, React, Rust, Kotlin, C#, Go 등은 비유에 쓰지 않는다
- Swift / SwiftUI / macOS / Xcode 는 처음
- → 설명 시 **Java / Spring Boot / Vue.js / JavaScript** 와의 비교/대조를 활용해 직관 형성

## 기술 스택

- macOS 15+ / SwiftUI / Swift 5
- Xcode 26.4.1 (`JHCalendar.xcodeproj`)
- Bundle ID: `com.jh.JHCalendar`

---

## 작업 흐름

### 사용자가 목표를 말하면 (예: "사이드바 만들자")

Claude 는 다음 순서로 수행한다:

1. `/docs/<번호>-<기능명>/` 폴더 생성
   - 번호: 본 파일 하단 **마스터 인덱스** 기준 다음 번호 (2자리, 예: `01`)
   - 폴더명에 한글 사용 OK
2. 폴더 안에 다음 파일을 생성:
   - `README.md` — 기능 요약 + 단계 체크리스트 + 의존 관계
   - `01-<단계명>.md`, `02-<단계명>.md` … — 각 학습 단계 상세 (풀세트 템플릿)
3. 본 파일 하단 **마스터 인덱스** 에 항목 추가

> 단계 md 는 **학습 가이드** (개념 + 힌트 + 부분 예시) 다. **정답 풀코드는 포함하지 않는다** — 실제 구현은 사용자가 직접 한다.

### 단계 분할 원칙

- 한 단계 = **한두 개의 Swift 개념 + 그것을 적용하는 작은 구현**
- 한 세션 (30분~1시간) 안에 끝낼 수 있는 분량
- 매 단계 끝에 **빌드 가능 + 시각/동작 확인 가능** 한 결과물이 나와야 함

### 학습 포인트 작성 원칙

단계 md 의 "Swift / SwiftUI 개념" 섹션은 다음 규칙을 따른다.

1. **이전 단계에서 이미 다룬 개념은 재설명하지 않는다.** 한 줄 링크로 대체:
   - ✅ `@State` → [01-타이틀바-호버표시/02-호버상태.md](../01-타이틀바-호버표시/02-호버상태.md) 참고
   - ❌ 같은 개념을 다시 한 문단으로 풀어쓰기
2. **그 단계에서 새로 추가되는 측면** 이 있으면 그 부분만 새로 서술한다.
3. **새로 배울 개념이 하나도 없는 단계** (이전에 다 배운 스킬만 조합) 라면 "Swift / SwiftUI 개념" 섹션을 **통째로 생략** 하고, "이런 작업을 이런 방식으로 한다" 수준의 **작업 가이드** 만 작성한다.
   - 학습 목표 → "작업 목표" 로 대체 가능
   - 구현 가이드 + 직접 구현하기(체크포인트) + 자가 점검 정도면 충분
4. 풀세트 템플릿(아래 B)을 그대로 채우려 하지 말고, **그 단계에 의미 있는 섹션만** 남긴다.

> 작성 직전에 마스터 인덱스 + 기존 단계 md 들을 훑어 어떤 개념이 이미 등장했는지 확인할 것.

### 단계 진행 사이클 (학습 → 직접 구현 → 리뷰)

각 단계 md 는 **새 세션 단독 진입을 가정** 하므로 self-contained 하게 작성한다.

**새 세션 시작 시 Claude**
1. 본 `CLAUDE.md` 자동 로드
2. 사용자가 지정한 단계 md 읽기
3. 해당 기능 폴더의 `README.md` 도 읽어 의존/이전 단계 산출물 확인
4. 단계 md 의 **학습 개념 + 힌트** 를 사용자에게 안내 (Claude 는 코드를 대신 작성하지 않는다)

**한 단계 진행 사이클**
1. **문서 작성** — Claude 가 단계 md 작성. **학습 개념 + 힌트 + 부분 예시** 만 담고, **정답 풀코드는 담지 않는다.**
2. **사용자 구현** — 사용자가 본인 손으로 코딩.
3. **리뷰 요청** — 사용자가 "다 했어" / "이렇게 했어" 등으로 보고.
4. **Claude 리뷰** — Claude 가 변경 파일을 읽고 다음 세 축으로 점검:
   - (a) **동작**: 학습 목표대로 동작/빌드되는가
   - (b) **스타일**: Swift 관용 표현/네이밍 OK 인가
   - (c) **학습 포인트**: 이 단계에서 배우려던 개념이 실제로 코드에 반영됐는가
   필요시 수정 제안 → 사용자가 다시 수정.
5. **완료 처리** — 단계 md 의 체크박스 + README.md 의 체크박스 + 마스터 인덱스 모두 갱신.

**힌트 노출 원칙**
- 사용자가 명시적으로 요청하기 전까지 **정답 풀코드를 먼저 제시하지 않는다.**
- 막힐 때 점진적으로: **추가 힌트 → 부분 정답 → 전체 정답** 순서로 노출.
- 리뷰 톤은 비판이 아닌 **학습 코칭** — 왜 이게 더 나은지, 가능하면 다른 언어와 비교.

### 진행 상태 추적

- 단계 완료 시 해당 `README.md` 의 체크박스를 `- [x]` 로 변경
- 마스터 인덱스의 상태 컬럼도 갱신 (`예정` / `진행중` / `완료`)

---

## 파일 템플릿

### A. `README.md` (기능 요약)

```markdown
# <기능명>

## 목표
<한두 줄로 무엇을 만드는지>

## 의존 관계
- 사전 필요: <다른 기능 또는 "없음">
- 이후 영향: <이 기능 위에 올릴 것들>

## 단계 체크리스트
- [ ] 01 - <단계명>
- [ ] 02 - <단계명>
- [ ] 03 - <단계명>

## 이 기능에서 학습할 Swift / SwiftUI 개념
- 개념 A
- 개념 B
```

### B. `NN-<단계명>.md` (단계 상세 — **풀세트**)

```markdown
# 단계 N: <단계명>

## 학습 목표
이 단계를 마치면 무엇을 할 수 있게 되는가. (1~3줄)

## 사전 지식
- 이전 단계 산출물 (어떤 코드/파일이 있어야 하는지)
- 알고 있어야 할 Swift 개념 (이전 단계에서 다룬 것 또는 외부 지식)

## Swift / SwiftUI 개념
이 단계에서 **새로** 배울 문법/개념만.
- 이전 단계에서 다뤘으면 재설명 X → 한 줄 링크 (`개념명 → [경로](상대경로)`)
- 새 개념이 하나도 없으면 이 섹션 자체를 **생략** (위 "학습 포인트 작성 원칙" 참고)
- 다른 언어와의 비교 포함 (예: "JS 의 X 와 같지만 Y 가 다르다")
- 가능하면 짧은 예시 코드 1~2개

## 구현 가이드
> 정답 풀코드는 제공하지 않는다. 사용자가 직접 작성.

- 어떤 파일을 만들/수정할지
- 어떤 API/타입을 사용할지 (이름·시그니처 수준)
- 핵심 골격만 보여주는 부분 스니펫 (`// TODO: 여기 채우기` 형태)
- 막힐 만한 지점에 대한 힌트

## 직접 구현하기
체크포인트 형식으로 작은 단위로 쪼갠다.
- [ ] 체크포인트 1
- [ ] 체크포인트 2
- [ ] 체크포인트 3

> 다 끝나면 "다 했어" / "이렇게 했어" 라고 알리면 Claude 가 리뷰.

## 자가 점검 (구현 후)
- 빌드 통과? (⌘B 또는 `xcodebuild ...`)
- 실행 시 어떻게 보여야 하는지 (예상 동작/모습)
- 이해도 퀴즈 1~2개 (자문자답)

## Claude 리뷰 체크리스트
*(Claude 가 리뷰 시 사용)*
- [ ] 학습 목표와 동작 일치
- [ ] Swift 관용 스타일/네이밍
- [ ] 이 단계의 학습 포인트가 코드에 반영됨

## 회고
- 막혔던 부분?
- 추가로 궁금했던 점?
> *(사용자가 단계 진행 후 직접 채우는 영역)*

## 조금 더 (선택)
관련 심화 주제. 원할 때 더 읽어볼 것.
```

---

## 디렉토리 구조

```
JH-CALENDAR/
├── CLAUDE.md                          ← 본 파일
├── JHCalendar.xcodeproj/
├── JHCalendar/                        ← 실제 소스
│   ├── JHCalendarApp.swift            ← 앱 진입점
│   ├── ContentView.swift              ← 루트 뷰 조합 (작게 유지)
│   ├── Features/                      ← 기능별 모듈
│   │   ├── TitleBar/
│   │   │   ├── TrafficLightHoverArea.swift   (호버 영역 + onHover 상태)
│   │   │   └── TrafficLightController.swift  (AppKit 트래픽라이트 제어)
│   │   └── <FeatureName>/             ← 새 기능은 이 아래에 폴더 신설
│   ├── Assets.xcassets/
│   ├── JHCalendar.entitlements
│   └── Preview Content/
└── docs/                              ← 학습 문서 루트
    ├── 01-타이틀바-호버표시/
    │   ├── README.md                  ← 기능 요약 + 체크리스트
    │   ├── 01-기본 타이틀바 숨기기.md
    │   └── ...
    └── 02-...
```

### 새 기능을 추가할 때

- `JHCalendar/Features/<FeatureName>/` 폴더를 새로 만들고 그 안에 Swift 파일을 둔다.
- **기존 폴더에 파일 추가** — `Sidebar` / `FloatingToolbar` / `MonthlyCalendar` 폴더는 `PBXFileSystemSynchronizedRootGroup` 으로 등록돼 있어 폴더에 파일을 두면 **자동 인식**. pbxproj 편집 불필요. (구식 `TitleBar` 폴더만 수동 등록 필요.)
- **새 폴더를 만드는 경우** — sync group 으로 등록하는 게 권장. `project.pbxproj` 의 `PBXFileSystemSynchronizedRootGroup section` 에 새 항목 추가 + 부모 그룹 children 에 참조 추가. 그러면 이후 파일 추가가 자유로움.
- 구식 방식 (PBXFileReference + PBXBuildFile + 그룹 children + PBXSourcesBuildPhase 4곳 수동 편집) 은 `TitleBar` 같은 기존 구식 폴더에 파일을 추가할 때만 필요.
- 등록 후 `xcodebuild -project JHCalendar.xcodeproj -scheme JHCalendar -configuration Debug build` 로 빌드 통과 확인.
- **루트 `ContentView` 는 가능한 작게 유지** — 컴포넌트 조합만 하고, 상태/AppKit 호출은 각 Feature 안으로.

---

## 마스터 인덱스

| 번호 | 기능 | 상태 | 위치 |
|---|---|---|---|
| 01 | 타이틀바 호버 표시 | 완료 | `docs/01-타이틀바-호버표시/` |
| 02 | 상단 플로팅 툴바 | 완료 | `docs/02-상단-플로팅-툴바/` |
| 03 | 사이드바 슬라이드 | 완료 | `docs/03-사이드바-슬라이드/` |
| 04 | 사이드바 카테고리 UI | 완료 | `docs/04-사이드바-카테고리-UI/` |
| 05 | 월간 달력 UI | 완료 | `docs/05-월간-달력-UI/` |
| 06 | 주말 색상 | 완료 | `docs/06-주말-색상/` |
| 07 | 사이드바 토글 다듬기 | 완료 | `docs/07-사이드바-토글-다듬기/` |
| 08 | 카테고리 추가 다이얼로그 | 진행중 | `docs/08-카테고리-추가-다이얼로그/` |
